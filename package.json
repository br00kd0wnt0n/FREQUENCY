{
  "name": "frequency",
  "version": "1.0.0",
  "description": "A narrative mystery experience through a simulated radio interface",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "postinstall": "npm run build -w shared",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "build:client": "npm run build -w shared && npm run build -w client",
    "build:server": "npm run build -w shared && npm run build -w server",
    "start": "npm run start -w server",
    "db:migrate": "npm run migrate -w server",
    "db:seed": "npm run seed -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
